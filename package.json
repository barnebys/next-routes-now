{
  "name": "next-routes-now",
  "version": "0.0.6",
  "description": "CLI tool to generate next-routes for Now 2.0",
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "yarn run build",
    "build": "ncc build lib/index.js"
  },
  "files": [
    "dist/index.js",
    "bin/generate-now-routes"
  ],
  "bin": {
    "generate-now-routes": "bin/generate-now-routes"
  }
}
